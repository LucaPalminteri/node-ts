{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Start",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/src/server.ts",
            "envFile": "${workspaceFolder}/.env",
            "outFiles": [
                "${workspaceFolder}/**/*.ts"
            ]
        }
    ]
}